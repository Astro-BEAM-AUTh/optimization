# Signal Analysis Done Quick

Small C implementations of a signal analysis pipeline algorithms.

## Source tree roadmap
* `ctom`: contains C sources of the "complex to magnitude" algoirthm

## Building

Use `make -C` to run the directory-specific Makefiles
and build the binaries.
See [ctom's Makefile](./ctom/Makefile) for the available
build rules.